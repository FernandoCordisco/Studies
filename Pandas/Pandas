Pandas

Dados Estruturados:

- Dados que podem ser usados em linhas, colunas e conter informações mais fáceis de manipulação
- XML, CSV, Etc

Dados Não Estruturados:

- Word, PDF, Imagem, MP4, Vídeo.

Jargões:

- Data Frame: Tabela;
- Registro, Exemplo, Observação, Amostra(Sample): Linha da data frame;
- Característica(Feature): Títulos da data frame, que pode ser usado como variável, atributo, etc;
- Índice: Número da linha (Pode ser textual);
- Series: Um conjunto de elementos, podendo ser linha ou coluna (Array/Vetor) (Lista);
- Variáveis Independentes: Atributo que não depende de outo dado;
- Variáveis Dependentes: Atributo que depende de outro dado, podendo juntar dados;
- Amostra: Subconjunto de um total.

Tipos de dados:

- Numéricos: Escalada numérica (Quantitativo):
- Discreto: Números inteiros associados à contagem;
- Contínuo: Float associados a medição.

Categóricos: Categória/grupos (Qualitativos):
- Nominal: Não há ordem entre categórias;
- Ordinal: Quando há ordem entre categórias;
- Identificador: Possui apenas um único indivíduo por categoria. PK.

- Texto: Descrições, textos maiores.
- Datas.

